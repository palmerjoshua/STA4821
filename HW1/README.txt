This program has a CLI to allow the user to re-run the simulation without needing to restart the program.
It also saves the output to a file.

The simulation function is toward the end of the script on line 62. 